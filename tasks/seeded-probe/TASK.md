# Deterministic seeded probe

Use this fixture to prove the ordinary success path. It must write `result.json` under the runner-provided `LAB_ARTIFACT_DIR`; the same `LAB_SEED` produces the same hash.

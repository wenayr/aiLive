# Task 02 — add OKX

In `project/src/exchanges.mjs`, add `adapters.okx`. It must accept the same quote
shape and return a normalized quote with `source: 'okx'`.

Before `npm run check:okx`, run the genetic scan. This second related change is
expected to select the instruction learned from Task 01 and request a focused
inspection. A clear result ends the cycle; only an issue needs an owner decision.
